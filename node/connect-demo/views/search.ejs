<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=s, initial-scale=1.0">
    <title>search</title>
</head>
<body>
    <h1>search results for <%= search %></h1></h1>
    <% if(results.length) { %>
        <ul>
            <% for(var i=0;i<results.length;i++) { %>
                <li><%= results[i].title %> - <%= results[i].date %></li></li>
            <%} %>
        </ul>
    <% } else { %>
        <p>No result</p>
    <% } %>
</body>
</html>